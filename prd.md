# bili-danmu — PRD

## 1. 项目概述

### 1.1 项目名称

**bili-danmu**

### 1.2 项目定位

bili-danmu 是一个运行于 Windows 的轻量级 B 站直播弹幕桌面客户端。

核心目标：

> **连接 B 站直播间 → 获取实时弹幕 → 在桌面透明悬浮层显示 → 可选进行弹幕朗读。**

项目重点不是做一个完整的直播客户端，而是做一个：

**轻量、低资源、低干扰、可高度自定义的桌面弹幕 Overlay。**

---

# 2. 重构目标

本项目是对现有 [DanmuFree](https://github.com/SoraYjy/DanmuFree) 项目的重新实现。

但**不要求完整复刻原项目所有功能**。

本次重构明确缩小功能范围：

### 必须支持

* B 站直播弹幕
* 弹幕实时显示
* 弹幕朗读（TTS）
* 桌面透明窗口
* 无边框窗口
* 窗口置顶
* 鼠标穿透
* 窗口自由调整大小
* 窗口自由移动
* Windows 桌面 Overlay
* 基础配置持久化

### 明确不支持

本版本暂时不实现：

* 抖音
* 虎牙
* 斗鱼
* YouTube
* Twitch
* 多平台聚合
* GPT-SoVITS
* 视频播放
* 直播画面
* 礼物动画系统
* 复杂插件系统
* 云端同步
* 用户账号体系
* 服务端
* 数据库

**第一阶段只关注 B 站 + 弹幕 + TTS + Overlay。**

---

# 3. 技术目标

## 3.1 推荐技术栈

```text
Frontend
├── Vue 3
├── TypeScript
└── Vite

Desktop
└── Tauri 2

Backend / Core
└── Rust

Platform
└── Windows / Win32 API
```

### 原则

Rust 负责：

* B 站连接
* WebSocket
* 数据解析
* 弹幕事件
* TTS 调度
* 配置管理
* Windows 窗口控制
* 鼠标穿透
* Overlay
* 快捷键
* 系统能力

Vue 负责：

* 设置界面
* 弹幕渲染
* 用户交互
* 状态展示
* UI 配置

---

# 4. 总体架构

```text
┌───────────────────────────────────────┐
│               Vue 3 UI                │
│                                       │
│  设置界面 / 弹幕 Overlay / 状态展示       │
└──────────────────┬────────────────────┘
                   │
                Tauri IPC
                   │
┌──────────────────▼────────────────────┐
│              Rust Core                │
│                                       │
│  ┌────────────┐   ┌───────────────┐  │
│  │ Bilibili   │   │ Event Manager │  │
│  │ Client     │──▶│               │  │
│  └────────────┘   └───────┬───────┘  │
│                           │           │
│              ┌────────────┼────────┐  │
│              ▼            ▼        ▼  │
│           Overlay        TTS     Config│
│                                       │
└───────────────────────────────────────┘
                   │
                   ▼
              Windows API
```

---

# 5. 核心功能

## 5.1 B 站直播间连接

用户输入：

```text
直播间 ID
```

例如：

```text
123456
```

点击：

```text
连接
```

程序连接 B 站直播间。

---

## 5.2 直播间连接状态

需要明确显示当前连接状态：

```text
未连接
连接中
已连接
连接断开
连接失败
正在重连
```

推荐 UI：

```text
● 已连接
```

断开：

```text
● 未连接
```

连接过程中：

```text
◌ 连接中...
```

---

# 6. B 站弹幕

## 6.1 弹幕事件

第一版本至少支持：

```text
普通弹幕
```

建议底层保留扩展能力：

```rust
enum BilibiliEvent {
    Danmaku(Danmaku),
    Gift(Gift),
    SuperChat(SuperChat),
    Guard(Guard),
    UserEnter(UserEnter),
    Like(Like),
}
```

但：

> **V1 只处理 `Danmaku`。**

其他事件可以暂时解析但不消费，也可以完全不实现。

---

# 7. 弹幕数据结构

统一为：

```rust
struct Danmaku {
    id: String,
    username: String,
    content: String,
    timestamp: i64,
}
```

如果 B 站协议中存在有价值的信息，可以额外保留：

```text
用户 UID
用户等级
勋章
舰队等级
颜色
发送时间
```

但这些不是 V1 必须展示的信息。

---

# 8. 弹幕 Overlay

这是整个项目的核心功能。

启动后可以打开一个：

```text
透明
无边框
置顶
桌面悬浮
```

的窗口。

例如：

```text
┌────────────────────────────────────────────┐
│                                            │
│  用户A：主播你好！                         │
│                                            │
│                  用户B：哈哈哈哈哈哈       │
│                                            │
│        用户C：今天直播什么？               │
│                                            │
└────────────────────────────────────────────┘
```

背景完全透明。

---

# 9. Overlay 窗口特性

## 9.1 无边框

不显示：

* 标题栏
* Windows 边框
* 最大化
* 最小化
* 关闭按钮

---

## 9.2 透明背景

窗口背景：

```text
RGBA(0, 0, 0, 0)
```

用户只能看到弹幕。

---

## 9.3 鼠标穿透

提供：

```text
鼠标穿透：开启 / 关闭
```

开启后：

> 鼠标事件应该穿透 Overlay，不影响下面的窗口。

例如：

```text
┌───────────────────────┐
│   弹幕 Overlay        │
│   鼠标完全穿透         │
└───────────────────────┘
          ↓
┌───────────────────────┐
│     游戏 / 浏览器      │
│     正常接收鼠标       │
└───────────────────────┘
```

这是**核心桌面功能**。

---

# 10. 鼠标穿透切换

需要支持：

```text
穿透 ON
穿透 OFF
```

关闭穿透后，可以：

* 移动窗口
* 调整窗口大小
* 操作 Overlay

---

# 11. 窗口置顶

提供：

```text
始终置顶
```

开启：

```text
Overlay
   ↓
始终位于普通窗口上方
```

关闭：

```text
Overlay
   ↓
遵循普通窗口层级
```

---

# 12. Overlay 尺寸

用户可以自由调整：

```text
宽度
高度
```

不限制固定比例。

同时提供预设：

```text
16:9
21:9
自定义
```

但 V1 实际上只需要：

> Windows 原生调整窗口大小。

---

# 13. Overlay 位置

用户可以自由移动窗口。

需要保存：

```text
X
Y
Width
Height
```

程序重新启动后恢复上一次位置。

---

# 14. 弹幕显示模式

### 列表弹幕

```text
用户A：哈哈哈哈

用户B：主播牛逼

用户C：来了来了

用户D：这个操作可以
```

从底部向上滚动。


---

# 15. 弹幕样式

至少支持：

```text
字体大小
字体颜色
字体透明度
字体
```

建议增加：

```text
文字描边
描边颜色
描边宽度
```

因为透明 Overlay 在复杂背景上非常容易看不清。

推荐默认：

```text
白色字体
黑色描边
```

---

# 16. 弹幕数量限制

必须考虑高速直播间。

不能无限制创建 DOM。

例如：

```text
最大同时显示弹幕：
50
100
200
```

超过限制后：

```text
移除最早弹幕
```

前端必须避免：

```text
几千 / 几万 DOM 节点
```

造成内存持续增长。

---

# 17. 弹幕过滤

V1 建议提供基础过滤：

```text
关键词过滤
```

例如：

```text
关键词：

广告
抽奖
加微信
```

包含这些关键词的弹幕：

```text
不显示
不朗读
```

---

# 18. 用户过滤

提供：

```text
屏蔽用户
```

例如：

```text
用户名 A
用户名 B
```

被屏蔽用户：

```text
不显示
不朗读
```

---

# 19. TTS 弹幕朗读

> 状态：已实现（Edge TTS 单一引擎）。朗读筛选用独立配置，与弹幕显示筛选互不影响；
> 协议细节见 `Task/findings.md`，验证脚本 `scripts/edge-tts-probe.ps1`。

## 实现说明（与下文设计的差异）

| 项 | 设计 | 实际实现 |
|---|---|---|
| 引擎抽象 `trait TtsEngine` | 多引擎可插拔 | **未做抽象**：只有 Edge TTS 一个实现，写在 `tts/edge.rs`；真的加第二引擎时再抽接口 |
| 朗读队列 | 单 worker 串行 | **流水线**：合成与播放拆成两个任务（容量 1 的通道衔接），播第 N 条时已在合成第 N+1 条 |
| 队列满时 | 丢弃最旧 | 同设计，另加「**积压时丢弃待播旧弹幕**」：作废还没开播的在途音频（带 1.8s 冷却与 `epoch` 代次），**正在念的那条念完**——音频整条合成，掐断只能听到半句 |
| 扫描用户 | 可以过滤用户 | **未做**（黑名单待实现） |
| 音色列表 | 从系统动态获取 | 从微软 `voices/list` 拉取，**只保留 `zh-` 前缀的 14 个中文音色**，缓存内存不落盘；离线回退内置同名单 |
| 朗读文案 | 未规定 | 默认只念正文；可开「念用户名」与「念身份前缀（房管/舰长）」 |
| 最大朗读长度 | 单条弹幕 × 字符 | 只约束**弹幕正文**，不含身份前缀与用户名 |

## 实测关键数据（决定默认值）

- 每条固定开销：握手 ~650ms + 合成首字节 ~230ms（共 ~0.9s），与文本长度无关
- 音频时长 ≈ **0.21 秒/字**；B 站弹幕上限 30–40 字 → 单条最坏 7–9 秒
- 所以默认 `max_len = 15`（正文 ≈ 3.4s），`max_queue = 5`，`interrupt_on_backlog = true`
- 换本地引擎（Windows SAPI 合成仅 26–88ms）**不会降积压延迟**，因为瓶颈是播放时长

这是第二个核心功能。

用户可以开启：

```text
☑ 开启弹幕朗读
```

收到弹幕后：

```text
B站
 ↓
Danmaku
 ↓
Filter
 ↓
TTS Queue
 ↓
语音播放
```

---

# 20. TTS 架构

TTS 不应该直接绑定某一个服务。

抽象：

```rust
trait TtsEngine {
    async fn speak(&self, text: &str) -> Result<()>;
}
```

V1 至少支持：

```text
Edge TTS
Windows 系统 TTS
```

> 实际交付：V1 只做了 Edge TTS（自研直连微软 Read Aloud WebSocket，无 SDK 依赖）；
> Windows 系统 TTS 作为离线兜底留待后续。

未来可以扩展：

```text
自定义 HTTP TTS
其他 TTS 服务
```

但 V1 不实现这些。

---

# 21. TTS 设置

至少提供：

```text
启用 TTS
音色
语速
音量
```

例如：

```text
音色：
Microsoft Xiaoxiao

语速：
1.0x

音量：
80%
```

具体可用音色应该从 Windows 系统动态获取。

---

# 22. TTS 队列

必须设计队列。

不能：

```text
收到弹幕 A
 ↓
立即 speak()

收到弹幕 B
 ↓
立即 speak()

收到弹幕 C
 ↓
立即 speak()
```

否则会产生：

```text
声音重叠
```

正确：

```text
A
↓
TTS Queue

B
↓
TTS Queue

C
↓
TTS Queue

       ↓

A → B → C
```

---

# 23. TTS 防刷屏

高速直播间可能：

```text
100 条弹幕 / 秒
```

不能全部朗读。

需要：

```text
TTS 最大队列长度
```

例如：

```text
20
```

超过：

```text
丢弃旧消息
```

或者：

```text
只保留最新消息
```

默认推荐：

> **队列超过限制时丢弃最旧的待朗读消息。**

---

# 24. TTS 内容清洗

朗读之前需要处理：

```text
Emoji
特殊字符
URL
重复字符
过长文本
```

例如：

```text
哈哈哈哈哈哈哈哈哈哈哈哈哈哈哈哈
```

不能完整朗读几十次「哈」。

应该做合理清洗。

同时限制：

```text
单条弹幕最大朗读长度
```

例如：

```text
100 字符
```

---

# 25. TTS 与显示分离

非常重要：

```text
弹幕显示
```

和：

```text
弹幕朗读
```

必须是两个独立消费者。

```text
             Danmaku
                 │
          ┌──────┴──────┐
          ↓             ↓
       Overlay         TTS
```

关闭 TTS：

```text
Overlay 正常运行
```

关闭 Overlay：

```text
TTS 仍然可以工作
```

---

# 26. 设置中心

主界面建议分为：

```text
┌─────────────────────────────┐
│ DanmuFree                   │
├─────────────┬───────────────┤
│             │               │
│ 直播间      │               │
│ 弹幕       │    Settings    │
│ 显示       │               │
│ 朗读       │               │
│ Overlay    │               │
│ 关于       │               │
│             │               │
└─────────────┴───────────────┘
```

---

# 27. 设置分类

## 直播间

```text
直播间 ID
连接状态
连接
断开
自动重连
```

### 状态仪表盘

直播间页底部只读展示当前运行状态（不改配置，修改入口仍在弹幕 / 朗读 / Overlay 页）：

```text
弹幕窗      显示中 · 穿透开 · 置顶开
弹幕速率    12 条 / 10 秒
朗读        已开启 · 只读 舰长/房管 或 粉丝牌
```

* 弹幕速率 = 最近 10 秒接收到的全部弹幕条数（不受显示 / 朗读筛选影响），断开连接后立即归零
* 朗读条件按身份规则拼接：身份规则之间是「或」，敏感词屏蔽独立叠加且不豁免
* 数据由 Rust 侧每秒聚合一次下发（`get_dashboard_status`），前端不做状态推算

---

## 弹幕

```text
显示弹幕
显示模式
字体
字体大小
字体颜色
透明度
速度
描边
最大弹幕数量
```

---

## 朗读

```text
启用 TTS
TTS 音色
语速
音量
最大队列
最大朗读长度
```

---

## Overlay

```text
透明窗口
鼠标穿透
始终置顶
窗口位置
窗口大小
```

---

# 28. 自动重连

B 站连接断开以后：

```text
Disconnected
      ↓
等待
      ↓
Reconnect
      ↓
成功 → Connected
      ↓
失败
      ↓
再次等待
```

采用：

```text
指数退避
```

例如：

```text
1s
2s
4s
8s
16s
30s
```

最大：

```text
30s
```

---

# 29. 配置持久化

使用本地配置文件。

不使用数据库。

推荐：

```text
JSON / TOML
```

保存：

```text
直播间 ID
Overlay 配置
字体配置
TTS 配置
过滤规则
窗口位置
窗口大小
```

配置目录应该遵循 Windows 推荐的数据目录。

不要把配置写在：

```text
exe 所在目录
```

---

# 30. 日志

Rust 后端需要统一日志系统。

至少：

```text
INFO
WARN
ERROR
DEBUG
```

关键事件：

```text
B站连接
B站断开
WebSocket 错误
重连
弹幕解析失败
TTS 错误
窗口操作失败
```

开发环境可以输出详细日志。

Release 默认减少日志噪音。

---

# 31. 异常处理

必须避免：

> 一条异常弹幕导致整个程序崩溃。

例如：

```text
B站发送未知协议
        ↓
解析失败
        ↓
记录日志
        ↓
忽略该消息
        ↓
继续运行
```

而不是：

```text
panic!
 ↓
程序退出
```

---

# 32. 性能目标

这是本项目非常重要的指标。

目标：

### CPU

空闲状态：

```text
尽可能接近 0%
```

普通弹幕：

```text
低 CPU 占用
```

---

### 内存

目标：

```text
正常运行 < 100MB
```

具体不做硬性保证，但需要避免：

```text
弹幕越多
内存无限增长
```

---

### 启动速度

目标：

```text
点击启动
 ↓
快速进入主界面
```

尽量做到：

```text
< 2 秒
```

具体以实际机器为准。

---

# 33. 包体积目标

这是重构的重要目标之一。

原项目：

```text
~260MB
```

bili-danmu：

> **目标安装包 ≤ 30MB**

理想目标：

```text
≤ 20MB
```

注意：

不把 WebView2 Runtime 打包进安装包。

使用 Windows 系统已有的 WebView2 Runtime。

如果目标机器没有 WebView2：

```text
提示用户安装
```

或者提供安装引导。

---

# 34. 单实例

程序默认只允许运行一个实例。

如果用户再次启动：

```text
检测已有实例
        ↓
激活已有窗口
        ↓
退出第二个实例
```

避免：

```text
两个程序同时连接 B站
```

---

# 35. 快捷键

建议 V1 支持：

```text
Ctrl + Shift + D
```

显示 / 隐藏 Overlay。

```text
Ctrl + Shift + T
```

开启 / 关闭 TTS。

```text
Ctrl + Shift + P
```

开启 / 关闭鼠标穿透。

具体快捷键后续可以调整。

---

# 36. 系统托盘

建议支持 Windows Tray。

程序关闭主窗口后：

```text
不退出程序
```

而是：

```text
最小化到托盘
```

托盘菜单：

```text
显示设置
显示/隐藏弹幕
开启/关闭 TTS
开启/关闭鼠标穿透
退出
```

---

# 37. 启动行为

支持：

```text
☑ 开机启动
```

以及：

```text
☑ 启动后自动连接上次直播间
☑ 启动后自动显示 Overlay
```

这些可以作为 V1.1。

---

# 38. 安全边界

本项目只连接：

```text
Bilibili
```

不应该：

* 执行远程代码
* 下载未知可执行文件
* 上传用户数据
* 默认收集遥测
* 默认上传日志

用户配置：

```text
仅存储在本机
```

---

# 39. 项目目录建议

```text
danmufree/
│
├── src/
│   ├── components/
│   ├── views/
│   ├── composables/
│   ├── stores/
│   ├── types/
│   └── App.vue
│
├── src-tauri/
│   ├── src/
│   │   ├── bilibili/
│   │   │   ├── client.rs
│   │   │   ├── protocol.rs
│   │   │   ├── parser.rs
│   │   │   └── event.rs
│   │   │
│   │   ├── tts/
│   │   │   ├── mod.rs
│   │   │   └── windows.rs
│   │   │
│   │   ├── overlay/
│   │   │   ├── mod.rs
│   │   │   └── windows.rs
│   │   │
│   │   ├── config/
│   │   │   └── mod.rs
│   │   │
│   │   ├── commands/
│   │   │   └── mod.rs
│   │   │
│   │   ├── state.rs
│   │   ├── error.rs
│   │   └── lib.rs
│   │
│   └── tauri.conf.json
│
├── public/
├── package.json
├── vite.config.ts
└── README.md
```

---

# 40. 开发原则

这是给 AI Agent 最重要的一部分。

## 原则 1：不要过度设计

当前版本只服务于：

```text
Windows
+
B站
+
弹幕
+
TTS
+
Overlay
```

不要提前实现：

```text
多平台
插件系统
云同步
数据库
账号系统
```

---

## 原则 2：Core 与 UI 解耦

Rust Core 不应该依赖 Vue UI 的具体实现。

核心逻辑：

```text
Bilibili
 ↓
Event
 ↓
Core
```

UI：

```text
Event
 ↓
Vue
```

---

## 原则 3：不要把业务逻辑放进 Vue

Vue 只负责：

```text
展示
交互
状态绑定
```

B站协议、重连、TTS 队列、窗口控制全部放 Rust。

---

## 原则 4：优先稳定而不是追求功能数量

宁可：

```text
B站弹幕
100% 稳定
```

也不要：

```text
B站
抖音
斗鱼
虎牙
YouTube

但全部半成品。
```

---

# 41. MVP 开发顺序

建议严格按照下面顺序开发。

### Milestone 1

```text
Tauri
+
Vue
+
Rust
```

完成：

* 项目初始化
* Rust/Vue IPC
* 基础 UI

---

### Milestone 2

```text
B站 WebSocket
```

完成：

* 连接直播间
* 接收数据
* 协议解析
* 普通弹幕

---

### Milestone 3

```text
Overlay
```

完成：

* 透明
* 无边框
* 置顶
* 鼠标穿透
* 移动
* 调整大小

---

### Milestone 4

```text
弹幕渲染
```

完成：

* 滚动弹幕
* 列表弹幕
* 样式
* 数量限制
* 性能优化

---

### Milestone 5

```text
TTS
```

完成：

* Windows TTS
* TTS Queue
* 过滤
* 防刷屏
* 文本清洗

---

### Milestone 6

```text
产品化
```

完成：

* 配置持久化
* Tray
* 自动重连
* 单实例
* 快捷键
* 日志
* Installer
* 自动更新

---

# 42. MVP 验收标准

当以下条件全部满足时，可以认为 MVP 完成。

### B站

* [ ] 输入直播间 ID 可以连接
* [ ] 可以稳定接收弹幕
* [ ] 可以正确显示用户名
* [ ] 可以正确显示弹幕内容
* [ ] 断线可以自动重连
* [ ] 异常消息不会导致程序崩溃

### Overlay

* [ ] 窗口透明
* [ ] 无边框
* [ ] 可以置顶
* [ ] 可以鼠标穿透
* [ ] 可以关闭鼠标穿透
* [ ] 可以移动
* [ ] 可以调整大小
* [ ] 可以保存位置
* [ ] 可以保存大小

### 弹幕

* [ ] 支持滚动模式
* [ ] 支持列表模式
* [ ] 可以修改字体
* [ ] 可以修改字体大小
* [ ] 可以修改颜色
* [ ] 可以修改速度
* [ ] 有最大弹幕数量限制
* [ ] 不会因为弹幕数量增加导致内存无限增长

### TTS

* [x] 可以开启/关闭
* [x] 可以选择音色（14 个中文音色：普通话 / 方言 / 粤语 / 台湾，可一键从微软刷新）
* [ ] 可以选择 Windows 系统音色（本地引擎未做，当前为 Edge TTS 在线音色）
* [x] 可以调整语速
* [x] 可以调整音量
* [x] 有 TTS 队列
* [x] 有队列长度限制（超出丢弃最旧）
* [x] 积压时丢弃待播旧弹幕（在播的那条念完，延迟上限 = 一条朗读时长）
* [x] 可以过滤关键词（敏感词命中整条不念）
* [x] 可以按身份筛选朗读（舰长/房管、粉丝牌、荣耀等级）
* [ ] 可以过滤用户（拉黑用户名）
* [x] 不会同时播放多个语音
* [x] 无内容弹幕不发声（纯标点 / 纯 emoji / 纯链接不念「某某说」）
* [x] 朗读内容清洗（控制字符、会念出名字的符号、链接、重复字符）

### 产品

* [ ] 支持系统托盘
* [ ] 支持单实例
* [ ] 配置可以持久化
* [ ] 程序异常不会直接崩溃
* [ ] Release 包尽可能小
* [ ] 不依赖 .NET Runtime
* [ ] 不依赖 Node.js Runtime
* [ ] 不内置 Chromium Runtime

---

# 43. 非目标

**本项目开发过程中，AI Agent 不应该自行添加以下功能：**

```text
❌ 抖音
❌ 斗鱼
❌ 虎牙
❌ YouTube
❌ Twitch
❌ 多账号
❌ 云同步
❌ 数据库
❌ Web 管理后台
❌ 用户系统
❌ AI 弹幕总结
❌ AI 自动回复
❌ GPT TTS
❌ GPT-SoVITS
❌ 插件市场
❌ 弹幕礼物特效
❌ 直播录像
❌ 视频播放器
```

除非用户明确提出，否则不得实现。

---

# 44. 最终产品形态

最终希望用户看到的是一个非常简单的工具：

```text
                 bili-danmu

┌──────────────────────────────────┐
│                                  │
│  B站直播间                       │
│                                  │
│  [ 123456                ]       │
│                    [连接]        │
│                                  │
│  ● 已连接                        │
│                                  │
│  ──────────────────────────────  │
│                                  │
│  弹幕              [开启]        │
│  朗读              [关闭]        │
│  鼠标穿透          [开启]        │
│  始终置顶          [开启]        │
│                                  │
└──────────────────────────────────┘
```

点击 Overlay 后：

```text
               游戏 / 浏览器

       用户A：主播这波可以啊

                    哈哈哈哈哈

           用户B：来了来了

  用户C：这个操作太帅了
```

背景完全透明。

鼠标穿透后：

```text
        ↓ 弹幕
─────────────────────────

        ↓ 鼠标直接穿透

─────────────────────────
        游戏正常操作
```

最终目标就是：

> **打开 → 输入房间号 → 连接 → Overlay 出现 → 弹幕开始飘 → 可选朗读。**

没有复杂的账号体系，没有多余功能，没有巨大的运行时。

---

## 给 AI Agent 的开发指令

你本地实际让 Codex / Pi Agent 开发时，我建议不要只把 PRD 扔给它，而是在 `AGENTS.md` 里再加一句非常关键的话：

> **严格按照 PRD 实现，不主动扩大需求范围。每完成一个 Milestone 后先运行测试并检查现有功能，不要在未确认的情况下引入额外框架、服务或复杂架构。涉及 Windows 原生窗口行为时，优先使用 Rust + Win32 API 实现，不使用 Node.js/Python 等外部 Runtime。**

另外我会建议你**不要让 Agent 一次性把整个项目写完**。这个项目特别适合你之前使用 Pi/Codex 的方式：让它按照 `Milestone 1 → 2 → 3...` 一层层做，每一阶段跑起来以后再进入下一阶段。

**尤其是第一阶段，我建议先别碰 UI 美化。先把「Rust 能稳定连接 B 站 → 收到弹幕 → Tauri Event 推给 Vue」这条链打通。** 这条链通了，后面基本就是往上叠 Overlay 和 TTS。
